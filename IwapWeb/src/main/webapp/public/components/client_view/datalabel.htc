<component>

<attach event="oncontentready" onevent="contentready()" />

<property name="extra" value="datalabel" />

<script>
	function contentready(){	
	  if(!element.dataset) 
	    element.dataset = element.form.dataset;
	  Control.initElementDataset(element);
	  element.window = window;
	}
	
</script>

</component>	